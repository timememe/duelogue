# Эксперимент единого вердикта — до интеграции

**Дата:** 2026-07-14

**Обновлено:** 2026-07-15

**Статус:** симуляционный полигон; `rules_core.gd` и `battle_controller.gd` не изменены.

**Сценарий:** `res://duelogue/tools/sim_verdict_formula.tscn`.

## 1. Проверяемая гипотеза

Вместо независимых условий «KO / TKO / решение сначала по ширине» матч получает один
финальный перевес:

```text
V = wР × (рамки_you − рамки_opp)
  + 1  × (тезисы_you − тезисы_opp)
  + 1  × зал
```

- знак `V` определяет победителя;
- точный `V = 0` в симе оставлен ничьёй, чтобы измерить частоту до выбора тай-брейка;
- зал — **независимое состояние**, а не производная доски;
- победитель каждого завершённого клинча получает `+1` зала (`−1`, если это OPP), кап `±5`;
- обычная выкладка рамки/тезиса зал не двигает: её уже оценивает доска;
- KO и TKO в сим-наследнике отключены, иначе часть партий не подчинялась бы проверяемой
  формуле;
- ноль своих рамок не завершает матч: сторона ещё может разбирать, красть или открыть новую.

Прогнаны прозрачные веса рамки `wР = 1 / 2 / 3`; тезис и пункт зала всегда стоят по 1.

## 2. Методика и оговорки

- Канон: `U3 T8 R9`, из атак две Кражи, рука 5, клинч+заморозка, гейт 2/4,
  захват закрытым трофеем со всеми тезисами.
- Smart-зеркало: 1200 матчей на конфиг.
- Поле стилей: 350 матчей на направленную пару.
- Архетипы обоймы и число Краж: 900 матчей на ячейку.
- Первый ход строго чередуется; для одинаковых ячеек используются одинаковые серии сидов.
- Производственный smart-бот **не переучен** под новую формулу: он всё ещё оптимизирует
  старую ширину. Поэтому поле стилей — сторож регрессии, а не окончательная оценка потолка.

## 3. Smart-зеркало

| Правило | Первый ход | Ничьи | Ходы | Захваты | Новый победитель против старого решения |
|---|---:|---:|---:|---:|---:|
| Текущий KO/TKO/ширина | 49.1% | 2.7% | 18.4 | 1.32 | — |
| `1Р + 1Т`, без зала | 57.3% | 8.3% | 23.3 | 1.46 | 7.8% |
| `1Р + 1Т + 1З` | 51.2% | 6.9% | 23.2 | 1.72 | 21.2% |
| `2Р + 1Т + 1З` | 50.4% | 5.3% | 23.2 | 1.72 | 16.8% |
| **`3Р + 1Т + 1З`** | **49.6%** | **5.8%** | **23.2** | **1.72** | **14.0%** |

Для `3Р + 1Т + 1З`:

- победы с меньшим числом рамок, но большим числом тезисов: 5.2%;
- победы с большим числом рамок, но меньшим числом тезисов: 5.1%;
- зал перевернул уже ненулевой вес доски: 3.3%;
- зал решил равную по весу доску: 5.2%;
- зал дошёл до капа всего в 0.4% матчей;
- средний `|зал|` в финале — 1.5 пункта.

**Чтение:** вес 3 впервые даёт симметричную конвертацию ширины и глубины. Независимый зал
реально участвует примерно в 8.5% решительных исходов, но не подавляет доску; кап `±10`
в первом прогоне дал практически те же результаты, поэтому `±5` достаточно.

Цена отказа от KO/TKO — `+4.8` хода, примерно `+26%` к длине матча. Раннее завершение
позже понадобится возвращать только как прозрачный математически гарантированный вердикт,
не как другую формулу победы.

## 4. Архетипы обоймы против канона

Обе стороны пилотирует smart; все обоймы содержат 20 карт и две Кражи.

| Обойма YOU | Старые условия | `1Р+1Т+1З` | `2Р+1Т+1З` | `3Р+1Т+1З` |
|---|---:|---:|---:|---:|
| Канон `3/8/9` | 50.1% | 50.1% | 50.3% | 50.4% |
| Глубина `2/12/6` | 37.3% | **63.8%** | 56.2% | **49.9%** |
| Ширина `5/7/8` | 48.5% | **38.3%** | 43.8% | **46.7%** |
| Разбор `2/6/12` | 52.7% | 59.2% | 56.7% | **55.3%** |
| Смешанная `4/9/7` | 53.3% | 47.8% | 50.8% | **53.3%** |

**Вердикт:** буквальная равная сумма `1+1+1` не годится — она заменяет диктат ширины
диктатом Тезисов. `3 рамка / 1 тезис / 1 зал` — первая здоровая точка: все проверенные
архетипы при фиксированных двух Кражах остаются примерно в коридоре 47–55%.

## 5. Блокер: двойной перенос Кражи

При `3Р+1Т+1З` рамка с одним тезисом весит 4. Захват удаляет эти 4 очка у владельца и
добавляет 4 захватившему — минимальный свинг равен 8. Фактический средний вес захваченной
рамки — 4.07, средний свинг — **8.13**, тогда как средний финальный `|V|` — 6.3.

Чувствительность обоймы YOU к числу Краж против канона K2:

| Кражи YOU | Старые условия | `3Р+1Т+1З` | Δ к K2 новой формулы |
|---:|---:|---:|---:|
| 0 | 31.1% | **27.8%** | −22.1 пп |
| 1 | 38.8% | **39.6%** | −10.3 пп |
| 2 | 47.7% | **49.9%** | 0 |
| 3 | 53.7% | **58.6%** | +8.7 пп |
| 4 | 58.4% | **65.5%** | +15.6 пп |

Это запрещает интеграцию формулы в текущую свободную сборку обоймы. Возможные следующие
гипотезы тестировать отдельно:

1. Кражи — фиксированные два системных слота, не часть свободного deckbuilding.
2. Захват перестаёт переносить полный вердиктный вес дважды (меняется сама добыча/роль
   трофея, но правило обязано остаться видимым на карте).
3. Кража становится редким именным приёмом, а базовая атака только снимает вес.

## 6. Поле политик — второй сторож

Средний винрейт стиля против остальных под `3Р+1Т+1З`:

| Tall | Wide | Aggro | Balanced | Smart | Разброс |
|---:|---:|---:|---:|---:|---:|
| 33% | 49% | 35% | 54% | **80%** | 47 пп |

Текущий smart поднимается с 70% при старых условиях до 80%. Частично это ожидаемо:
одномерные стили намеренно карикатурны, а smart играет экономику и пороги. Но до интеграции
нужен новый verdict-aware бот и повторный paired-seed прогон; нельзя объявлять 80% чистым
«скиллом игрока».

## 7. Решение по эксперименту

**Не интегрировать сейчас.** Формула доказала фундаментальную ценность, но не готова целиком.

Что уже подтверждено:

- единый числовой вердикт работает и меняет осмысленную долю исходов;
- независимый зал `±1 за клинч`, кап `±5` значим, но не доминирует;
- первый ход остаётся честным;
- `3Р + 1Т + 1З` балансирует проверенные wide/tall/aggro-составы при фиксированных K2;
- ширина и глубина могут компенсировать друг друга в одной прозрачной системе.

Что блокирует интеграцию:

- Кража становится главным регулятором винрейта из-за двойного переноса веса;
- smart-политика не соответствует новой целевой функции;
- матч без отдельных KO/TKO длиннее на четверть;
- точные ничьи около 5.8%: автоматическая отдача их одной роли через burden of proof даст
  этой роли примерно столько же пунктов преимущества и требует отдельной компенсации.

Следующий тест должен сначала решить контракт Кражи/фиксированных слотов, затем переучить
сим-бота под вердикт. Только после этого имеет смысл ручной плейтест и интеграция в ядро.

## 8. Продолжение: fixed K2 + verdict-aware бот

**Гипотеза:** убрать Кражи из свободного deckbuilding (у обеих сторон всегда ровно K2) и
повторить `3Р+1Т+1З` политикой, которая прямо читает новый перевес. Сим-бот:

- оценивает состояние как `3×рамки + тезисы + зал`;
- выбирает цели по вердиктному весу на требуемое число чипов;
- обязательно закрывает доступный захват;
- в отставании предпочитает teardown, в лидерстве — капитализацию рамок/тезисов;
- принимает решения клинча с учётом веса теряемой рамки и текущего перевеса;
- холдит каждую из фиксированных K2 до окна захвата.

Это отдельный стиль `verdict` внутри сим-полигона, production `ai.gd` не изменён.

### 8.1 Политика действительно адаптировалась

На одинаковых сериях сидов против старого smart:

| Verdict как YOU | Verdict как OPP | Средний винрейт |
|---:|---:|---:|
| 73.5% | 73.3% | **73.4%** |

Значит старый smart действительно недооценивал новую целевую функцию; предыдущий тест был
консервативным. Но адаптация вскрыла две новые проблемы.

### 8.2 Сильное преимущество второго слова

Verdict-зеркало, 900 матчей:

| YOU wins | Первый ход | Ничьи | Ходы | Захваты |
|---:|---:|---:|---:|---:|
| 44.6% | **34.7%** | 10.1% | 19.2 | 1.96 |

Первый ход перестал быть честным. Вероятная причина — реактивная ценность новой суммы:
первый создаёт видимый вес, второй немедленно получает оптимальную teardown-цель и последнее
слово в экономике. Эвристика `отстаёшь → атакуй` может усиливать эффект, поэтому пока нельзя
объявить все 15 пп чистым дефектом правил; нужен отдельный свип инициативы и более глубокая
оценка темпа.

### 8.3 Fixed K2 недостаточно для свободы обойм

Обе стороны используют verdict-политику; у всех обойм ровно K2:

| Обойма YOU | Винрейт против канона |
|---|---:|
| Канон `3/8/9` | 49.8% |
| Глубина `2/12/6` | 54.7% |
| **Ширина `5/7/8`** | **38.5%** |
| Разбор `2/6/12` | 57.8% |
| Смешанная `4/9/7` | 55.1% |

При оптимизации новой суммы чистая ширина снова становится ловушкой: дополнительная рамка
весит 3, но уменьшение числа ответов/Тезисов делает её слишком легко конвертируемой целью.
Фиксация K2 убрала лестницу «возьми больше Краж», но не убрала власть самого захвата и
реактивного teardown над тонкими рамками.

### 8.4 Обновлённый вердикт

**По-прежнему не интегрировать.** Эксперимент стал точнее:

- `3Р+1Т+1З` остаётся перспективной общей формулой;
- независимый зал и единый подсчёт выдержали смену политики;
- fixed K2 необходим, но недостаточен;
- новая оптимальная политика создаёт серьёзный second-player advantage;
- широкая обойма падает ниже целевого коридора.

Следующий тест должен разделить две причины, не меняя сразу всё:

1. Сначала исправить/проверить инициативу: paired-seed свип стартового зала `+0/+1/+2` первому
   и альтернативной verdict-политики, считающей оставшийся боезапас, а не только текущий вес.
2. Затем при честном первом ходе сравнить две версии Кражи: текущий двойной перенос против
   одинарного teardown без присоединения вердиктного веса.

До прохождения обоих сторожей ручной плейтест будет оценивать заведомо перекошенную версию.

## 9. Изоляция инициативы и гейта

### 9.1 Стартовый зал не чинит первое слово

В verdict-зеркале проверен независимый стартовый зал относительно первого игрока от `−4`
до `+2`. Доля побед первого хода:

| Стартовый зал первого | −4 | −3 | −2 | −1 | 0 | +1 | +2 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| Победы первого | 40.8% | 41.8% | 41.2% | 40.1% | 35.7% | 33.5% | 28.7% |

Положительный стартовый зал парадоксально усиливает преимущество отвечающего: текущий гейт
считает сторону с меньшей поддержкой андердогом и расширяет ей окно захвата. Отрицательный
зал помогает первому лишь частично и одновременно резко увеличивает число состояний у капа
зала (`12.3%` при `−4` против `5.6%` при `−3`). Стартовая фора залом не является честным
исправлением инициативы.

### 9.2 Гейт не является корнем перекоса

Verdict-зеркало при fixed K2 и `3Р+1Т+1З`:

| Гейт | Первый ход | Ничьи | Ходы | Захваты |
|---|---:|---:|---:|---:|
| Выключен | 38.4% | 6.9% | 19.3 | 1.94 |
| `3/5` | 39.4% | 7.9% | — | 1.97 |
| `2/4` | 38.3% | 8.1% | — | 2.02 |

Для обойм против канона:

| Обойма | Без гейта | `3/5` | `2/4` |
|---|---:|---:|---:|
| Глубина | 55.1% | 55.7% | 55.8% |
| Ширина | 25.9% | 31.9% | 34.3% |
| Разбор | 61.2% | 60.9% | 59.4% |

Выключение гейта не возвращает инициативу и ещё сильнее наказывает широкую обойму. Гейт
усиливает отдельные эффекты, но не объясняет second-player advantage.

## 10. Порог реактивного teardown

Проверено, не создаёт ли перекос одна эвристика `отстаёшь → атакуй`. Всё остальное
зафиксировано: fixed K2, `3Р+1Т+1З`, гейт `2/4`.

| Реакция на текущий минус | Первый ход | Ничьи | Против smart | Wide → canon | Ходы | Захваты |
|---|---:|---:|---:|---:|---:|---:|
| Атаковать при `V < 0` | 35.4% | 7.2% | 73.5% | 32.9% | 19.3 | 2.10 |
| Атаковать при `V ≤ −5` | 32.4% | 8.3% | 71.4% | 34.4% | 19.4 | 2.33 |
| Атаковать при `V ≤ −9` | 33.6% | 9.7% | 69.7% | 35.9% | 19.4 | 2.54 |
| Не атаковать только из-за минуса | 32.7% | 9.4% | 71.1% | 37.7% | 19.3 | 2.57 |

Ни один вариант не проходит одновременно три сторожа: `45–55%` первого хода,
преимущество над старым smart и wide не ниже `40%`. Простая правка реактивного порога не
объясняет мету. Но это также не является опровержением формулы: одноступенчатая эвристика
оценивает только заранее придуманные политики и не умеет находить каскад лучших ответов.
Следующий автоматический уровень — lookahead/MCTS либо популяционный best-response тест.

## 11. Семантическая гипотеза после симуляций

Тесты политики не должны подменять тест смысла формулы. Рабочее чтение содержательной части:

```text
L(side) = Σ по оставшимся рамкам (3 + число оставшихся за рамкой тезисов)
```

Алгебраически это `3Р+1Т`, но причинно это не два независимых мешка очков. Рамка — якорь и
критерий релевантности; тезис — сохраняющееся содержательное обязательство внутри рамки.
Тезис без рамки не должен иметь финальной цены. Пустая рамка может стоить 3 только если само
состояние «рамка осталась» уже означает минимально установленную и защищённую позицию.

Зал семантически ближе не к ещё одному тезису, а к восприятию аргумента. Поэтому следующая
гипотеза **пока только для отдельного полигона, не для production**:

```text
P_you = L_you × M(H)
P_opp = L_opp × M(−H)
Verdict = P_you − P_opp
```

`H` — нормализованная общая реакция зала, а `M(H)` — простой положительный ограниченный
множитель, например `1 + αH` при стартовом свипе `α = 0.15 / 0.20 / 0.25`. Делитель не нужен:
обратный множитель выражает ту же зависимость менее прозрачно и опаснее ведёт себя у нуля.

Семантические сторожа этой версии:

- зал не создаёт позицию из ничего: `L = 0 → P = 0`;
- при равном содержании реакция зала способна решить спор;
- максимальная реакция не отменяет заранее оговорённый крупный содержательный разрыв;
- игрок может до вердикта объяснить победу через удержанные рамки, тезисы и их восприятие;
- лучшие ответы образуют узнаваемые риторические стратегии, а не требуют объяснения через
  числовой эксплойт тайминга, гейта или последнего хода.

Таким образом, текущие результаты не отклоняют направление `3Р+1Т`. Они отклоняют
преждевременный вывод о балансе из зеркала одного бота и ставят под вопрос **аддитивную** роль
зала. Хороший каскад — тот, где формула остаётся публичной грамматикой, а возникающие
контрстратегии можно естественно описать как формы реального риторического поведения.

## 12. Как воспроизвести завтра

Production-правила не менялись. Все команды запускают только
`res://duelogue/tools/sim_verdict_formula.tscn`:

```powershell
$godot = 'D:\DOWNLOADS\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64_console.exe'

# Полный исходный свип весов, архетипов и Краж
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn

# Fixed K2: verdict-aware зеркало, cross-play и архетипы
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --verdict-ai

# Изоляция стартового зала, гейта и порога реактивной атаки
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --initiative-only
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --gate-only
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --policy-threshold
```

Для ручного теста текущая ветка предоставляет полигон и наблюдения, но не меняет игровой
экран и не интегрирует новый вердикт в боевое ядро.

## 13. Продолжение: доска → напряжение → реакция → зал

**Статус:** отдельный симуляционный слой внутри `sim_verdict_formula.gd`; production-ядро,
`EmotionCore`, `rules_core.gd` и `battle_controller.gd` не менялись.

Проверена причинная цепочка, в которой три системы имеют разные роли:

```text
карта → исход на доске → напряжение → видимая реакция/разрядка
      → один публичный расчёт сцены → независимый зал → гейт следующей сцены
      → финальный V = 3Δрамки + Δтезисы + зал
```

- доска хранит содержание;
- напряжение — только риск поведения и **не входит в финальный счёт напрямую**;
- зал больше не производен от доски: он хранит память завершённых публичных сцен;
- итог клинча сначала полностью разрешается, затем проигравший получает эмоциональный
  stimulus, проходит возможная цепочка реакции/парировки и только после этого зал меняется
  один раз;
- новый зал действует со следующего события и не меняет задним числом исход клинча;
- победитель клинча даёт базовый `±1`; реакционная карта добавляет напечатанный
  `−1 / 0 / +1`; холодная парировка даёт `+1` парировавшему; сырой итог сцены ограничен;
- общий зал по-прежнему ограничен `±5` и читает гейт `2/4`.

Первичная карточная индексация для `volatile_default`:

| Реакция | Эффект относительно реактора |
|---|---:|
| Поиск свидетелей, Вспышка | `+1` |
| Переход на личности, Трещина | `−1` |
| Саркастические аплодисменты, Нервный смешок, Заело | `0` |
| Холодная парировка оппонента | `+1` оппоненту |

Это не финальный авторский знак каждой карты, а первая симметричная контрольная разметка:
примерно 29% выпавших реакций помогают реактору, 28% вредят, 43% нейтральны.

### 13.1 Контроли индексации

Verdict-зеркало, fixed K2, `1200` матчей/вариант:

| Индексация | Первый ход | Ничьи | Реакций/матч | Эмоция изменила знак финального `V` на той же доске |
|---|---:|---:|---:|---:|
| Только `±1` победителю клинча | 33.6% | 7.4% | 0 | 0 |
| Каждый срыв штрафует реактора | 37.3% | 7.2% | 8.83 | 7.6% |
| Карточные `−1/0/+1`, сцена `±2` | 37.5% | 7.2% | 8.91 | 12.3% |
| Только карточные реакции, без голоса клинча | 38.4% | 7.8% | 8.88 | 17.0% |
| Карточные эффекты, ровно один голос `±1` за сцену | 37.3% | 8.5% | 8.88 | 12.0% |
| Реакции и шкалы видны, но их эффект на зал равен 0 | 35.0% | 6.9% | 8.82 | 0 |

Ограничение сцены до `±1` **не решило** провокацию и срезало сырой результат 18.2% сцен
против 3.0% при `±2`. Оно хуже объясняет эффектную сцену и чаще скрывает вклад карты, поэтому
контроль `±1` отклонён. Отсутствие базового голоса клинча, напротив, слишком сильно отдаёт
зал случайной субколоде реакций (`17%` переклассификаций).

### 13.2 Провокация — не вторая атака

Добавлена политика `verdict_provoke`: при напряжении оппонента `≥4` она намеренно открывает
и дожимает клинч. В первом прогоне абсолютный результат `55–57%` выглядел как потенциальная
доминанта. Нулевой контроль показал, что это в основном **информационная ценность шкалы**:
даже когда реакции имеют нулевой эффект на зал, провокатор получает сопоставимый или больший
винрейт. Высокое напряжение коррелирует с уже проигранными сценами и служит дополнительной
эвристикой выбора момента атаки.

Длинный paired-seed прогон, `3600` матчей/ячейку:

| Когда добавляется давление клинча | Реакций/матч | Emotion-flip | Первый ход | Provoke, карточный зал | Provoke, эффект реакций 0 | Чистая механическая дельта |
|---|---:|---:|---:|---:|---:|---:|
| Каждая полная пара | 8.84 | 13.7% | 37.3% | 56.5% | 57.1% | **−0.6 пп** |
| Только первая полная пара | 6.77 | 13.7% | 37.1% | 56.8% | 57.8% | **−1.0 пп** |
| Только итог клинча | 3.05 | 12.8% | 37.5% | 54.3% | 54.4% | **−0.0 пп** |
| Итог; затяжное поражение получает `+1` интенсивности | **3.95** | **11.9%** | **37.1%** | **55.6%** | **56.7%** | **−1.1 пп** |

Карточный эффект не усиливает provoke-политику ни в одном варианте. Значит система пока не
становится скрытой второй атакой. Повторный нагрев каждой пары, однако, создаёт почти девять
катсцен за матч без роста смыслового влияния. Лучший причинный и темповый вариант:

> Во время ралли шкала не проверяется. После исхода проигравший получает обычную
> интенсивность события; если клинч пережил хотя бы одну дополнительную пару, поражение
> получает ещё `+1`. Затем возможна одна обычная цепочка реакции и единый расчёт зала.

### 13.3 Сторожа выбранного кандидата

Кандидат: `3Р+1Т+H`, зал `±5`, базовый голос клинча `±1`, карточный эффект `−1/0/+1`,
сцена `±2`, затяжное поражение `+1` интенсивности.

Гейт, `1200` матчей:

| Гейт | Первый ход | Ничьи | Реакций/матч | Захватов | Средний `|H|` | Emotion-flip |
|---|---:|---:|---:|---:|---:|---:|
| Выключен | 38.4% | 7.0% | 3.90 | 1.95 | 1.5 | 12.8% |
| `2/4` | 37.2% | 7.6% | 3.96 | 2.05 | 1.5 | 13.5% |

Архетипы против канона, `900` матчей/ячейку; обе стороны — verdict-политика, fixed K2:

| Обойма YOU | Без эмоций | Кандидат | Чистая дельта эмоций | Реакций/матч |
|---|---:|---:|---:|---:|
| Канон `3/8/9` | 51.0% | 49.7% | −1.3 пп | 3.93 |
| Глубина `2/12/6` | 56.0% | 55.8% | −0.2 пп | 2.58 |
| Ширина `5/7/8` | 34.0% | 33.8% | −0.2 пп | 4.18 |
| Разбор `2/6/12` | 60.0% | 57.9% | −2.1 пп | 5.61 |
| Смешанная `4/9/7` | 51.9% | 50.5% | −1.4 пп | 3.32 |

Эмоциональный слой не создаёт новую архетипную доминанту; максимальная дельта — `2.1 пп`,
причём она слегка охлаждает уже сильный Разбор. Но слой также намеренно **не лечит** старые
блокеры: второй ход всё ещё доминирует, wide остаётся около 34%, а гейт этого не исправляет.

### 13.4 Решение

**Причинную цепочку принять как следующий ручной прототип, но не интегрировать победу в
production.** Подтверждено:

- напряжение разумно работает как медиатор поведения, а не третья валюта победы;
- независимый событийный зал можно прозрачно соединить с доской одним расчётом на сцену;
- карточные эмоциональные знаки дают заметные `≈12%` переклассификаций, не доминируя над
  архетипами и не награждая provoke-политику дополнительным уроном;
- выбранная частота даёт около четырёх реакций за матч вместо девяти;
- гейт может читать новый зал, но не является лекарством инициативы.

До ручного теста механический знак будущей реакционной карты обязан быть публичным: лицо
следующей карты или точное множество возможных `−1/0/+1`. Текущий сим-бот видит шкалу, но
не планирует вокруг раскрытой конкретной карты, поэтому проверяет безопасность причинной
цепочки, а не потолок осознанной эмоциональной игры.

Воспроизведение:

```powershell
$godot = 'C:\Users\User\Desktop\GAMEDEV\Godot_v4.6.2-stable_win64.exe\Godot_v4.6.2-stable_win64_console.exe'

# Все контроли индексации, provoke, гейт и архетипы
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --emotion-chain

# Частота нагрева; --long = 3600 матчей/ячейку
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --emotion-pressure --long

# Финальный кандидат: гейт и парные архетипы без эмоций/с эмоциями
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --emotion-candidate
```

## 14. Разделение доски и зала: векторный исход

**Статус:** гипотеза проверена только в изолированном симуляторе. Production-правила и UI
не менялись.

Главное изменение относительно раздела 13: зал больше не является слагаемым победы.

```text
Logic B = 3·Δрамки + Δтезисы
Debate winner = sign(B)

Audience = (Lean, Heat)
Lean ∈ [−5; +5] — к кому склоняется публика
Heat ∈ [0; 3] — насколько публика сейчас вовлечена
```

Такой финал намеренно может быть расколотым: один оратор логически выиграл дебаты, но другой
забрал зал. Это не ничья и не скрытая сумма двух валют, а два разных ответа на два разных
вопроса.

### 14.1 Прозрачное обновление маятника

Проверенный кандидат `emotion reframes spectacle`:

1. Обычная выкладка меняет только доску; для зала это тихий такт `spectacle = 0`.
2. Обычный исход клинча сам по себе также не назначает любимца публики.
3. Зрелищным считается снятие/захват рамки, затяжной клинч или видимая эмоциональная реакция;
   такой такт имеет `spectacle = 2`.
4. Если ненейтральной реакции нет, зрелищный исход склоняет зал к победителю сцены.
5. Если случилась ненейтральная реакция/разрядка, она **переосмысляет сцену для публики**:
   её знак определяет публичную валентность вместо логического исхода.
6. Тихий такт остужает `Heat`, но не переписывает уже сложившийся `Lean`.

Формально для публичного события:

```text
Heat' = clamp(Heat + spectacle − 1, 0, 3)
Lean' = clamp(Lean + valence · (1 + Heat'), −5, +5)
```

где `valence ∈ {−1, 0, +1}`. Поэтому одинаковое событие при холодном зале сдвигает маятник
на `1`, а при максимальном накале — на `4`. Формула не требует складывать очки доски и зала.

### 14.2 Длинное сравнение моделей

Verdict-зеркало, гейт выключен, `3600` одинаковых сидов на модель. Во всех строках доска,
число ходов и захваты совпали — менялась только память аудитории.

| Память аудитории | `|Lean|` | `Heat` | Разворотов/матч | `corr(B, Lean)` | Вместе | Раскол | Нейтрально | Диагн. reclass | Состояний зала при частом одинаковом `B` |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| **Эмоция переосмысляет зрелище** | **2.92** | **0.66** | **1.56** | **0.296** | **59.2%** | **34.1%** | **6.7%** | **5.5%** | **39** |
| Только зрелищный исход доски | 2.80 | 0.66 | 1.51 | 0.387 | 62.0% | 30.2% | 7.8% | 5.0% | 40 |
| Зрелище с возвратом `Lean` к нулю | 0.85 | 0.66 | 2.98 | 0.213 | 29.2% | 14.9% | 55.8% | 3.6% | 27 |
| Старый накопитель голосов | 1.59 | 0 | 0 | 0.407 | 55.2% | 25.0% | 19.8% | 0 | 11 |
| Маятник без усиления `Heat` | 0.17 | 0.66 | 4.30 | 0.110 | 11.0% | 5.6% | 83.4% | 0.5% | 12 |
| `Lean×Heat`, возврат к нулю | 1.19 | 0.66 | 3.94 | 0.115 | 33.4% | 23.4% | 43.2% | 3.2% | 32 |
| Каждый клинч, без возврата к нулю | 2.70 | 0.66 | 1.94 | 0.255 | 57.0% | 35.0% | 8.0% | 5.3% | 39 |

`corr(B, Lean)` не должен быть ни около `1` (зал дублирует доску), ни обязательно около `0`
(зал выглядит случайным). У выбранного варианта связь умеренная, но `34.1%` логически
решённых партий дают расколотый исход, а самый частый одинаковый счёт доски `B = −1`
закончился `39` разными комбинациями `Lean/Heat`. Это сильный признак самостоятельной памяти.

Столбец `Диагн. reclass` показывает, что вспомогательная сумма
`B + sign(Lean)·Heat` изменила бы лишь `5.5%` логических исходов. Использовать её как победную
формулу не предлагается: она нужна только как тест силы эмоционального слоя.

### 14.3 Обратная связь и провокация

Выбранный маятник, `3600` матчей:

| Гейт зала в доску | Первый ход | Ничья по `B` | Захватов/матч | Раскол | `corr(B, Lean)` |
|---|---:|---:|---:|---:|---:|
| Выключен | 36.7% | 9.2% | 2.05 | 35.9% | 0.263 |
| `2/4` | 40.4% | 8.2% | 2.33 | 40.0% | 0.191 |

Гейт не смешивает финальные счета напрямую, но заметно меняет будущую доску: добавляет
`0.28` захвата за матч и `4.1 пп` расколотых исходов. Поэтому для первого ручного теста лучше
выключить его и проверить чистую читаемость двух шкал; после этого вернуть как отдельную
петлю `зал → доступность рискованного захвата`. Инициативный перекос он всё равно не лечит.

Парная provoke-проверка на тех же `3600` матчах:

| Индексация реакции | Победы провокатора по доске | Средний `Lean` к провокатору | `Heat` | Реакций/матч |
|---|---:|---:|---:|---:|
| Карточные `−1/0/+1`, реакция переосмысляет сцену | 54.4% | **+0.11** | 0.55 | 3.80 |
| Те же реакции, но публичный эффект карты равен `0` | 54.4% | **+0.22** | 0.55 | 3.80 |

Эмоциональная индексация не меняет логическую победу и отдельно отнимает у провокатора
`0.11 Lean`: публичная цена существует, но не превращается во вторую атаку. В среднем эффект
мал, потому что положительные, отрицательные и нейтральные карты симметрично гасят друг друга;
ценность системы должна проявляться в конкретных раскрытых реакциях и управлении моментом.

### 14.4 Рабочее решение

Для следующего ручного прототипа принять:

- победитель дебатов определяется **только** знаком `3·Δрамки + Δтезисы`;
- зал выводится отдельно как `Lean + Heat`, без финального сложения с доской;
- обычные аргументы принадлежат доске, зал индексирует только зрелищные исходы и реакции;
- ненейтральная реакция может переосмыслить для публики логический исход сцены;
- `Heat` усиливает амплитуду маятника, тихие ходы его остужают;
- гейт `зал → доска` сначала выключен и тестируется вторым шагом.

Нерешённый параметр — скорость охлаждения. Средний финальный `Heat = 0.66`, и только `17%`
матчей заканчиваются при `Heat ≥ 2`: это может правдиво отражать выдохшийся финал, а может
слишком быстро обнулять вовлечение. Его следует менять только после просмотра реального темпа
нескольких ручных партий, не одновременно с семантикой `Lean`.

Воспроизведение:

```powershell
# Все модели, 1200 матчей/ячейку
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --crowd-pendulum

# Выбранный кандидат, 3600 матчей/ячейку
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --crowd-pendulum --selected-only --long
```

## 15. Production-полигон отношений систем

**Статус:** архитектурный каркас и ручной UI подключены. Значения профилей остаются
экспериментальными; теперь их можно менять без переписывания трёх базовых систем.

### 15.1 Границы ответственности

```text
RulesCore / доска ─────────────┐
EmotionCore / раздражение ─────┼→ OutcomeEvaluator → финальный протокол
AudienceCore / Lean + Heat ────┘
          │
          └── опциональный link → старый API zal() → гейт / TKO
```

- `RulesCore` владеет только картами, рамками, тезисами, клинчем и доской;
- `EmotionCore` владеет двумя независимыми шкалами strain и субколодами реакций;
- `AudienceCore` получает только уже завершённые публичные сцены и хранит `Lean/Heat`;
- `OutcomeEvaluator` ничего не мутирует: читает единый снимок и применяет выбранный профиль;
- `OutcomeProfiles` — data-only реестр весов, связей и терминальных условий.

Экспериментальный зал подключается к прежнему `RulesCore.zal()` через явный внешний вход.
Поэтому существующие гейт, UI и AI могут читать новый зал, но сама доска не начинает владеть
его формулой. В Legacy-профиле вход выключен и `zal()` снова является старой производной
`рамки + сила + bias`.

Нокаут последней рамки также стал параметром профиля. В векторных вариантах он выключен:
сторона без рамки, но с легальным Разбором, может продолжить и дождаться общего вердикта.
Legacy сохраняет прежний немедленный KO.

### 15.2 Доступные ручные профили

Пауза → **Условия победы**. Переключение начинает новую партию, чтобы один матч никогда не
менял причинный контракт посередине.

Во время партии header показывает живой `B` с рамками/тезисами, бар зала подписан
`Lean + Heat`, а две шкалы strain остаются по сторонам сцены. Поэтому ручной тест видит все
три входа тем же языком, которым их затем объясняет финальный протокол.

| Профиль | Победитель | Зал → доска | Crowd TKO | Board KO |
|---|---|---|---|---|
| **Вектор: доска решает** (default) | `sign(3ΔР + ΔТ)` | выкл | выкл | выкл |
| Вектор + гейт `2/4` | `sign(3ΔР + ΔТ)` | захваты | выкл | выкл |
| Диагностика: логика + мандат | `sign(B + sign(Lean)·Heat)` | выкл | выкл | выкл |
| Диагностика: логика + Lean | `sign(B + Lean)` | выкл | выкл | выкл |
| Legacy | ширина → старый зал | гейт `2/4` | `10/3` | вкл |

Реестр хранится в `core/outcome/outcome_profiles.gd`. Симы и smoke-тесты получают те же
словари через `all()` / `get_profile()`, а ручной бой читает выбранный id из `Profile.settings`.

### 15.3 Атомарная сцена аудитории

Контроллер открывает транзакцию в начале клинча, накапливает реакции/парировки всей цепочки,
ждёт финальную реплику и только затем один раз вызывает `AudienceCore.resolve_scene()`.
Новый Lean не может задним числом изменить закончившийся клинч; гейт увидит его только в
следующем событии. Обычная Установка/Тезис вызывает отдельный тихий такт охлаждения.

Ненейтральные карты индексируются данными профиля:

```text
Поиск свидетелей, Вспышка  → +1 реактору
Переход на личности, Трещина → −1 реактору
остальные реакции → 0
спокойная парировка → +1 парировавшему
```

### 15.4 Новый финальный протокол

Старую вспышку «зал ваш» заменяет модальное scene-authored окно с одним immutable report:

- активный профиль и точная формула;
- `B` и разложение на рамки/тезисы с весами;
- `Lean` и `Heat`, не спрятанные в total;
- обе финальные шкалы раздражения;
- явная маркировка `РАСКОЛ`, если доска и публика выбрали разных ораторов;
- вердикт и описание того, какая система действительно определила победителя.

Кнопка «Остаться у доски» позволяет сверить протокол с финальным столом; «Новая партия»
перезапускает матч. Старый `match_ended(winner, reason, verdict)` оставлен для совместимости,
а новый `match_reported(report)` несёт полный векторный итог.

### 15.5 Автоматические сторожа

`outcome_architecture_smoke` проверяет профили, формулу маятника, reaction-priority,
переключение victory-policy на одном снимке, внешний зал для гейта, отключаемый board-KO и
контракт финального UI. Дополнительно пройдены прежние emotion/opening/run/simulation suites.

```powershell
& $godot --headless --path . res://duelogue/tools/outcome_architecture_smoke.tscn
& $godot --headless --path . res://duelogue/tools/emotion_controller_smoke.tscn
& $godot --headless --path . res://duelogue/tools/opening_controller_smoke.tscn
& $godot --headless --path . res://duelogue/tools/run_smoke.tscn
& $godot --headless --path . res://duelogue/tools/sim_verdict_formula.tscn -- --crowd-pendulum --selected-only
```
