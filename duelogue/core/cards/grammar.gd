extends RefCounted

## DUELOGUE — КОМБО-ГРАММАТИКА: общая риторическая онтология (контракт combo_grammar_v0.2 §1, §3).
## Нейтральный модуль данных без зависимостей (кроме card_types) — читается deck (тегирование
## при создании), narrative (реплики HIT/MISS), правилами, UI и AI из ОДНОГО источника.
## Словарь намеренно русский — «за основу берётся уже работающий словарь narrative engine»:
## схемы Тезисов («Аналогия»), приёмы Разборов («Контрпример») и зацепки («исключение»).
## Narrative держит свои старые name-мапы только как fallback для архивных данных.
##
## Ступень 1 лестницы (§13): чистые данные + matcher, БЕЗ механических эффектов.
## Механика (LINK/ARMED/CONFIRMED/BREAK) появляется на ступени 2 и читает эти же функции.

const C := preload("res://duelogue/core/cards/card_types.gd")

# --- Схемы Тезисов → масть (§1.1). Масти — данные будущих сетов, в ступени 1 инертны. ---
const SUIT_OF := {
	"Пример": "logos", "Авторитет": "ethos", "Статистика": "logos", "Аналогия": "logos",
	"Здравый смысл": "ethos", "Эмоция": "pathos", "Определение": "logos", "Традиция": "ethos",
}

# --- Приём Разбора → зацепка (§1.2). Кража — не зацепка, а присвоение: её здесь нет. ---
const HOOK_OF := {
	"Источник?": "источник", "Контрпример": "исключение", "Ложная аналогия": "сходство",
	"До абсурда": "следствие", "Передёрг": "подмена", "Корреляция": "связь",
	"Не в кассу": "уместность",
}

# --- Схема → открытые зацепки: может ли зацепка содержательно ударить по схеме (§1.2). ---
const OPEN_HOOKS := {
	"Авторитет": ["источник", "уместность", "исключение"],
	"Статистика": ["источник", "связь", "исключение"],
	"Пример": ["исключение", "источник", "сходство"],
	"Аналогия": ["сходство", "исключение"],
	"Традиция": ["следствие", "уместность"],
	"Здравый смысл": ["источник", "следствие"],
	"Эмоция": ["уместность", "подмена"],
	"Определение": ["подмена", "следствие", "источник"],
}

# --- ANSWER_OF[setup_scheme][hook] → маршрут (§1.4). OPEN_HOOKS определяет правильность
# атаки, ANSWER_OF — чем на неё отвечать в контексте атакованной схемы. ПОЛНЫЙ КАТАЛОГ:
# каждая из 20 содержательных пар матрицы имеет канонический риторический ответ — любой
# содержательный HIT открывает маршрут. Ответ обязан отвечать на critical question схемы,
# а не быть уловкой; masti ответов распределены по всем 8 схемам, чтобы каждая карта
# обоймы участвовала хотя бы в одном маршруте. Формулировки — кандидаты плейтеста. ---
const ANSWER_OF := {
	"Авторитет": {
		# «Кто источник?» → независимые данные подтверждают эксперта.
		"источник": {"answer_schemes": ["Статистика"],
			"route_id": "source_backed", "combo_name": "Источник подтверждён"},
		# «Он не по этой теме» → определить границы вопроса: ровно его область.
		"уместность": {"answer_schemes": ["Определение"],
			"route_id": "domain_match", "combo_name": "Эксперт по делу"},
		# «Эксперты ошибались» → консенсус большинства весит больше одного случая.
		"исключение": {"answer_schemes": ["Статистика"],
			"route_id": "expert_consensus", "combo_name": "Консенсус сильнее"},
	},
	"Статистика": {
		# «Чьи цифры?» → методика и институт с репутацией ручаются.
		"источник": {"answer_schemes": ["Авторитет"],
			"route_id": "vouched_numbers", "combo_name": "Цифры с подписью"},
		# «Корреляция ≠ причинность» → показать механизм на живом случае/сравнении.
		"связь": {"answer_schemes": ["Пример", "Аналогия"],
			"route_id": "mechanism_shown", "combo_name": "Механизм на столе"},
		# «Вот случай против» → статистика и есть про правило, не про исключение.
		"исключение": {"answer_schemes": ["Здравый смысл", "Статистика"],
			"route_id": "outlier_dismissed", "combo_name": "Исключение — не правило"},
	},
	"Пример": {
		# «А вот обратный пример» → мой случай типичен: вот распределение.
		"исключение": {"answer_schemes": ["Статистика"],
			"route_id": "typical_case", "combo_name": "Пример типичен"},
		# «Этот случай вообще был?» → задокументирован, есть свидетельства.
		"источник": {"answer_schemes": ["Авторитет"],
			"route_id": "documented_case", "combo_name": "Случай задокументирован"},
		# «Твой пример не о том» → по определению это тот же класс случаев.
		"сходство": {"answer_schemes": ["Определение"],
			"route_id": "same_class", "combo_name": "Тот же класс"},
	},
	"Аналогия": {
		# «Ложная аналогия» → назвать существенный общий признак сравнения.
		"сходство": {"answer_schemes": ["Определение"],
			"route_id": "shared_core", "combo_name": "Суть сходства"},
		# «А вот исключение» → авторитет подтверждает: случай учтён (канон §5).
		"исключение": {"answer_schemes": ["Авторитет"],
			"route_id": "exception_noted", "combo_name": "Исключение учтено"},
	},
	"Традиция": {
		# «Доведём до абсурда» → вернуть определением границы обычая (канон §5).
		"следствие": {"answer_schemes": ["Определение"],
			"route_id": "borders_restored", "combo_name": "Возвращаю границы"},
		# «Традиция ни при чём» → она работает сегодня, и людям она дорога.
		"уместность": {"answer_schemes": ["Пример", "Эмоция"],
			"route_id": "living_tradition", "combo_name": "Традиция жива"},
	},
	"Здравый смысл": {
		# «Кто эти "все"?» → общее место измерено: опросы и частоты.
		"источник": {"answer_schemes": ["Статистика"],
			"route_id": "commonly_measured", "combo_name": "Общее место измерено"},
		# «До абсурда» → мера очевидна: это как с привычным делом — никто не доводит.
		"следствие": {"answer_schemes": ["Аналогия", "Здравый смысл"],
			"route_id": "measured_sense", "combo_name": "Здравая мера"},
	},
	"Эмоция": {
		# «Не в кассу» → чувства и есть суть: вот живые люди (канон §5).
		"уместность": {"answer_schemes": ["Пример"],
			"route_id": "about_people", "combo_name": "Это касается людей"},
		# «Давишь на жалость» → чувство опирается на факты: вот масштаб в числах.
		"подмена": {"answer_schemes": ["Статистика"],
			"route_id": "grounded_feeling", "combo_name": "Чувство с фактурой"},
	},
	"Определение": {
		# «Определил удобно себе» → это словарное/научное определение, не моё.
		"подмена": {"answer_schemes": ["Авторитет"],
			"route_id": "by_the_book", "combo_name": "По словарю"},
		# «Из определения абсурд» → так его никто не применяет: границы очевидны.
		"следствие": {"answer_schemes": ["Здравый смысл"],
			"route_id": "sane_bounds", "combo_name": "Границы очевидны"},
		# «Откуда такое определение?» → устоявшееся употребление: так понимали всегда.
		"источник": {"answer_schemes": ["Традиция"],
			"route_id": "established_usage", "combo_name": "Устоявшееся значение"},
	},
}

# --- Тегирование при создании (deck.make_card): имя колоды → схема/приём. Авторитет —
# ДАННЫЕ КАРТЫ, не повторное вычисление по имени (§1.3); эти мапы — только фабричные. ---
const CARD_SCHEME := {
	"Довод": "Здравый смысл", "Контрфакт": "Статистика", "Аргумент": "Определение",
	"Уточнение": "Аналогия", "Пример": "Пример", "Ссылка": "Авторитет",
	"Факт": "Традиция", "Логика": "Эмоция",
}
const CARD_DEVICE := {
	"Не в кассу": "Не в кассу", "Передёрг": "Передёрг", "Контрпример": "Контрпример",
	"Софизм?": "До абсурда", "Источник?": "Источник?", "Подмена": "Корреляция",
	"Мимо": "Ложная аналогия", "А докажи": "Источник?",
}


# --- Чистый matcher (§3). Работает над объектами карт; сторожа §12: combo_eligible=false
# никогда не входит в грамматику, независимо от полей схемы. ---

static func eligible(card: Dictionary) -> bool:
	return bool(card.get("combo_eligible", false))


## Зацепка атаки: только у eligible Разбора с протегированным приёмом. Кража и «И что?»
## (безхуковый safe poke) возвращают "" и в грамматику не входят.
static func hook_of(attack: Dictionary) -> String:
	if not eligible(attack) or String(attack.get("type", "")) != C.TYPE_RAZBOR:
		return ""
	return String(HOOK_OF.get(String(attack.get("device", "")), ""))


## HIT(T, A): зацепка атаки содержательно берёт схему exact Тезиса.
static func hit(thesis: Dictionary, attack: Dictionary) -> bool:
	if not eligible(thesis) or String(thesis.get("type", "")) != C.TYPE_TEZIS:
		return false
	var hook := hook_of(attack)
	if hook == "":
		return false
	return (OPEN_HOOKS.get(String(thesis.get("scheme", "")), []) as Array).has(hook)


## ROUTE(T, A): запись ANSWER_OF для пары схема×зацепка; {} если маршрута нет.
static func route(thesis: Dictionary, attack: Dictionary) -> Dictionary:
	if not hit(thesis, attack):
		return {}
	var by_hook: Dictionary = ANSWER_OF.get(String(thesis.get("scheme", "")), {})
	return by_hook.get(hook_of(attack), {})


## HAS_ROUTE(T, A): HIT с известным маршрутом ответа — будущее открытие LINK.
static func has_route(thesis: Dictionary, attack: Dictionary) -> bool:
	return not route(thesis, attack).is_empty()


## ANSWERS(T, A, T2): правильный защитный ответ закрывает маршрут пары.
static func answers(thesis: Dictionary, attack: Dictionary, answer: Dictionary) -> bool:
	if not eligible(answer) or String(answer.get("type", "")) != C.TYPE_TEZIS:
		return false
	var r := route(thesis, attack)
	if r.is_empty():
		return false
	return (r.get("answer_schemes", []) as Array).has(String(answer.get("scheme", "")))


## TRIPLE(T, A, T2): минимальная тройка setup → зацепка → ответ (§0.1).
static func triple(thesis: Dictionary, attack: Dictionary, answer: Dictionary) -> bool:
	return answers(thesis, attack, answer)
